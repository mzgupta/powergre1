import {
  dictionaryLink
} from 'powergre/helpers/dictionary-link';

module('DictionaryLinkHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = dictionaryLink(42);
  ok(result);
});
